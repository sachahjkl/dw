# HE `ogf-form` / `ogf-form-block`

- Use: only when touching old `ogf-form` / `ogf-form-block` system.
- Main: `[REDACTED]`.
- Demo entry: `[REDACTED]`.
- Rule: maintenance pattern only. Not template for new modern `ogf-form-field` zone.
