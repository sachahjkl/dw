# HA écran métier riche avec formulaire

- Use: gros écran HA avec formulaire + services + état local.
- TS: `[REDACTED]`.
- Why: `input.required`, `output`, `signal`, `toSignal`, reactive forms, orchestration multi-services.
- HTML: `[REDACTED]`.
- Why: `[REDACTED]-title`, `[REDACTED]-button`, `mat-form-field`, `@if`, vraie structure métier.
- Constants: `[REDACTED]`.
- Why: `[REDACTED]CourseStepType`, `CourseStepTypeDurations`, `DateTimeFormat`.
