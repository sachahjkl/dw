# HA écran métier riche avec formulaire

- Use: gros écran HA avec formulaire + services + état local.
- TS: `src/app/gesco/funeral-customization/funeral-customization.component.ts`.
- Why: `input.required`, `output`, `signal`, `toSignal`, reactive forms, orchestration multi-services.
- HTML: `src/app/gesco/funeral-customization/funeral-customization.component.html`.
- Why: `gesco-title`, `gesco-button`, `mat-form-field`, `@if`, vraie structure métier.
- Constants: `src/app/gesco/constants.ts`.
- Why: `GescoCourseStepType`, `CourseStepTypeDurations`, `DateTimeFormat`.
